<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Messenger</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="icon" href="favicon.ico">
</head>
<body>
    <%- include('header') %>
    <main>
        <h2>Latest Posts</h2>
        <hr>
            <% posts.forEach(function(post) { %>
                <article>
                    <h3><%- post.title %></h3>
                    <p><%- post.content.replace(/\n/g, '<br>') %></p>
                    <p><small>Posted on (UTC): <span data-post-date="<%- post.date %>"></span> by <%- post.author %></small></p>
                </article>
                <hr>
            <% }); %>
    </main>
    <%- include('footer') %>
    <script src="/dateConversion.js" defer></script>
</body>
</html>